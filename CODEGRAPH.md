# ChatWorks

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-07-02 17:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

ChatWorks is a desktop chat application built with the Tauri framework, combining a Rust backend with a web-based frontend. The Rust backend handles core application responsibilities: securely storing API authentication tokens via the system keyring, managing LLM/AI model configuration parameters (temperature, top_p, max_tokens, system prompts), persisting conversation history as JSON files on disk, and exposing Tauri commands that the frontend consumes. The frontend (web UI) provides the chat interface and communicates with the backend through Tauri's inter-process communication layer. The application is a single integrated project — the Rust backend and web frontend are coupled within the Tauri build system rather than being independent deployable units. It depends on an external AI/LLM API (accessed via configurable authentication tokens) for generating responses. No other repositories appear to depend on it.

## Projects

### ChatWorks

ChatWorks is a desktop chat application built with Tauri (Rust backend + web frontend) that interfaces with an AI/LLM API. The Rust backend manages application settings (API authentication tokens, model sampling parameters like temperature/top_p/max_tokens, system prompts), persists conversations to disk, and exposes commands to the frontend. It uses the system keyring for secure token storage and stores conversations as JSON files.

