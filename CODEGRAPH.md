# ChatWorks

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 21:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

ChatWorks is a self-hosted local LLM inference server that exposes an OpenAI-compatible chat completion API. The repository implements a single cohesive application with the following technical responsibilities: (1) downloading and caching language models from Hugging Face Hub, (2) loading and quantizing those models for local inference, (3) managing model lifecycle through an actor-based engine architecture that handles concurrent requests, and (4) serving chat completions via an HTTP API that is protocol-compatible with OpenAI's API surface. The project appears to be a single-project repository where all components work together as one backend service — model import utilities feed into a model cache, the actor-based engine wraps model loading and text generation, and an HTTP layer routes incoming chat requests to the engine. It has no detected cross-repository dependencies, meaning it is intended to be a standalone, self-contained service. Downstream consumers would be any client that speaks the OpenAI chat API format (e.g., frontends, CLI tools, or other services that need LLM completions) pointed at the local server rather than OpenAI's hosted endpoint.

## Projects

### ChatWorks

ChatWorks is a local LLM (Large Language Model) inference server application that exposes an OpenAI-compatible chat API. It manages model loading, quantization, and text generation through an actor-based engine architecture, and supports importing models from Hugging Face. The project appears to serve as a self-hosted AI chat backend that can download, cache, and serve language models.

