# ChatWorks

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 20:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

ChatWorks is a local AI chat application that implements an OpenAI-compatible HTTP API server for running large language models (LLMs) on local hardware. The repository contains a single cohesive project that handles model loading and management, inference execution, and streaming token generation. Key technical responsibilities include: importing models from Hugging Face, applying quantization for reduced memory footprint, exposing chat and completion endpoints that mirror the OpenAI API contract, and managing configurable sampling parameters (e.g., temperature, top-p). Because it implements the OpenAI API interface, existing clients and tooling built against OpenAI's API can be redirected to this local server without code changes. There are no cross-repository dependencies identified; the project appears to be self-contained, functioning as both the inference backend and the API layer simultaneously.

## Projects

### ChatWorks

ChatWorks is a local AI chat application that provides an OpenAI-compatible API server for running language models locally. It manages model loading, inference, and streaming, with support for importing models from Hugging Face, quantization, and configurable sampling parameters. The project appears to serve as both a backend engine and API layer that exposes locally-run LLMs through an OpenAI-compatible interface.

