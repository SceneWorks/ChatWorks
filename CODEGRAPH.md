# ChatWorks

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-30 17:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

ChatWorks is a Tauri-based desktop application that provides a chat interface to an AI language model API. The repository contains a single project with a Rust backend (Tauri) and a web-based frontend. The backend is responsible for: managing persistent application settings (sampling parameters such as temperature, top-p, etc., and system prompts), securely storing API authentication tokens via the OS keyring, persisting conversation history as files on disk, and serializing/deserializing both settings and conversation data. Configuration is validated before use. All backend functionality is exposed to the frontend through Tauri's command system (IPC bridge), allowing the web UI to invoke Rust functions for operations such as loading/saving conversations, updating settings, and authenticating with the AI API. The project has no identified cross-repository dependencies and does not appear to be consumed as a library by other repositories. It depends on the Tauri framework, OS-level keyring APIs, and an external AI language model API endpoint.

## Projects

### ChatWorks

ChatWorks is a Tauri-based desktop chat application (Rust backend, likely a web frontend) that interfaces with an AI language model API. The backend manages persistent application settings (sampling parameters, system prompts, API authentication tokens stored via the OS keyring), and stores conversation history as files on disk. The project handles serialization/deserialization of settings and conversations, validates configuration, and exposes functionality to the frontend via Tauri commands.

